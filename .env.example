# MGMU DID Degree Verification — Environment Configuration
# Copy this file to '.env' and modify variables as needed.

# Frontend Configuration
VITE_API_BASE_URL=http://localhost:5000

# Backend Configuration (Optional fallback for local deployment)
JWT_SECRET=super_secret_project_key_123
PORT=5000
